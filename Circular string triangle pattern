#include<stdio.h>
#include <string.h>

int main()
{
int n,i,j,k=0;
char a[10000];
scanf("%d %s",&n,a);
for(i=0;i<n;i++)
{
    for(j=0;j<=i;j++)
    {
        printf("%c",a[k]);
        k++;
        if(k==strlen(a))
        k=0;
    }
    printf("\n");
}
}
